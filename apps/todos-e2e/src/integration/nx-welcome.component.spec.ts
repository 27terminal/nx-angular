describe('todos', () => {
  beforeEach(() => cy.visit('/iframe.html?id=nxwelcomecomponent--primary'));
  it('should render the component', () => {
    cy.get('angular-pro-nx-welcome').should('exist');
  });
});